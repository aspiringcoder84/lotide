function tail(arrayElements)
{ 
  return  arrayElements.slice[1];
   
}
//ddconst result = tail(["Hello", "Lighthouse", "Labs"]);
console.log(tail["Lighthouse", "Labs"]); // => will always fail!

// TEST CODE

//assertEqual("Lighthouse Labs", "Bootcamp");
//assertEqual(1, 1);
